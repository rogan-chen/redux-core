import * as types from '../action-types';

const actions = {
    // 数值加一的动作
    increment: { type: types.INCREMENT2 },

    // 数值减一的动作
    decrement: { type: types.DECREMENT2 }
}

export default actions;